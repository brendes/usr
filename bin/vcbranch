#!/bin/sh

if git rev-parse --is-inside-work-tree >/dev/null; then
    git rev-parse --abbrev-ref HEAD 2>/dev/null
elif hg id >/dev/null; then
    hg identify -b 2>/dev/null
else
    echo "Not in a git or hg repo"
fi
