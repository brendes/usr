# Cross-platform config

```
usr
.
├── bin # scripts and compiled binaries
├── etc # files that go in $HOME with a dot in front of their names
├── lib # other data: bookmarks, other configs, fonts, snippets etc
└── src # source code for compiled utilities
```
